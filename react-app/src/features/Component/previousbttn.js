import React from 'react';

const PreviousButton = ({ href }) => {
    return (
        <a href={href} className="previous">
            &#8249;
        </a>
    );
};

export default PreviousButton;
